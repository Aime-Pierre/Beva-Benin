# BEVA — Site officiel

Bénin Vivi Académie の公式サイト（静的HTML 1枚構成）。

## 公開手順（GitHub Pages）
1. このフォルダの中身をリポジトリにpush
2. Settings → Pages → Deploy from a branch → `main` / `/ (root)` → Save
3. 数分後 `https://<ユーザー名>.github.io/<リポジトリ名>/` で公開

## 更新方法
- 文言修正: `index.html` を直接編集してpush
- Open Classチラシ差し替え: `images/open-class-affiche.jpg` を同名で置き換え
- 写真差し替え: `images/` 内の該当ファイルを同名で置き換え

## 連絡先設定
- WhatsAppリンクは `wa.me/2290159717192` で全箇所統一（変更時は一括置換）
